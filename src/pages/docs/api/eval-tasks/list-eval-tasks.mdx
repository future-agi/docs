---
title: "List Eval Tasks"
description: "Retrieves a list of all eval tasks for the authenticated user's organization."
---

# List Eval Tasks

Retrieves a list of all eval tasks for the authenticated user's organization.

<ApiPlayground
  method="GET"
  endpoint="/tracer/eval-task/"
  baseUrl="https://api.futureagi.com"
  parameters={[]}
/>

## Authentication

All requests require authentication via API keys:

| Header | Description |
|--------|-------------|
| `X-Api-Key` | Your API key |
| `X-Secret-Key` | Your secret key |

## Responses

### 200

Successfully retrieved the list of eval tasks.

- **status**: boolean
  Indicates success.
- **result**: array
  Array of eval task objects, each containing `id`, `name`, `project`, `status`, `filters`, `sampling_rate`, `spans_limit`, `run_type`, `evals`, `created_at`, `updated_at`, `last_run`.

### 401

Unauthorized. Authentication credentials were not provided or are invalid.

### 500

Internal Server Error. An unexpected error occurred.

## Code Examples

### cURL

```bash
curl -X GET "https://api.futureagi.com/tracer/eval-task/" \
  -H "X-Api-Key: YOUR_API_KEY" \
  -H "X-Secret-Key: YOUR_SECRET_KEY" \
  -H "Content-Type: application/json"
```

### Python

```python
import requests

url = "https://api.futureagi.com/tracer/eval-task/"
headers = {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
    "Content-Type": "application/json"
}

response = requests.get(url, headers=headers)
print(response.json())
```

### JavaScript

```javascript
const response = await fetch("https://api.futureagi.com/tracer/eval-task/", {
  method: "GET",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
    "Content-Type": "application/json"
  },
});

const data = await response.json();
console.log(data);
```
