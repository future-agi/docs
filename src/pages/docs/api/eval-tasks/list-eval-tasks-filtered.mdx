---
title: "List Eval Tasks"
description: "Retrieves a paginated list of eval tasks for your organization, with optional filtering by project or task name. Use this endpoint to browse and manage your evaluation tasks."
---

<ApiPlayground
  method="GET"
  endpoint="/tracer/eval-task/list_eval_tasks/"
  baseUrl="https://api.futureagi.com"
  parameters={[
    {"name": "project_id", "in": "query", "required": false, "description": "Filter by project UUID.", "type": "string"},
    {"name": "name", "in": "query", "required": false, "description": "Search by task name (case-insensitive partial match).", "type": "string"},
    {"name": "page_number", "in": "query", "required": false, "description": "Page number (default: 0).", "type": "integer"},
    {"name": "page_size", "in": "query", "required": false, "description": "Results per page (default: 30).", "type": "integer"}
  ]}
  responseExample={{
    metadata: { total_rows: 1 },
    table: [{ id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890", name: "my-eval-task", status: "running", sampling_rate: 100.0, created_at: "2026-03-15T10:30:00Z" }],
    config: {}
  }}
  responseStatus={200}
  responseStatusText="OK"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Query parameters">
  <ParamField query="project_id" type="string" optional>
    Filter the results to only return eval tasks belonging to a specific project. Pass the project's UUID. This parameter also accepts `projectId` as an alias.
  </ParamField>
  <ParamField query="name" type="string" optional>
    Search for eval tasks by name. The search is case-insensitive and performs a partial match, so a query like `"hallucination"` will match tasks named `"hallucination-check"`, `"Hallucination Eval"`, etc.
  </ParamField>
  <ParamField query="page_number" type="integer" optional>
    The zero-based page number to retrieve. Defaults to `0` (the first page). Use in combination with `page_size` to paginate through large result sets.
  </ParamField>
  <ParamField query="page_size" type="integer" optional>
    The number of eval tasks to return per page. Defaults to `30`. Increase this value if you need to retrieve more results in fewer requests.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={200} statusText="OK">
  <ResponseField name="metadata" type="object">
    Pagination metadata for the result set. Contains `total_rows`, which indicates the total number of eval tasks matching the applied filters across all pages. Use this to calculate the total number of pages for your pagination UI.
  </ResponseField>
  <ResponseField name="table" type="array">
    An array of eval task objects representing the current page of results.
  </ResponseField>
  <ApiCollapsible title="Show 8 properties">
    <ResponseField name="id" type="string">
      The unique identifier (UUID) of the eval task.
    </ResponseField>
    <ResponseField name="name" type="string">
      The human-readable name assigned to the eval task.
    </ResponseField>
    <ResponseField name="status" type="string">
      The current execution status of the eval task.
    </ResponseField>
    <ResponseField name="filters_applied" type="object">
      The query filters applied to narrow which spans are eligible for evaluation.
    </ResponseField>
    <ResponseField name="created_at" type="datetime">
      The ISO 8601 timestamp of when the eval task was created.
    </ResponseField>
    <ResponseField name="evals_applied" type="integer">
      The number of evaluation configs attached to this task.
    </ResponseField>
    <ResponseField name="sampling_rate" type="number">
      The percentage of eligible spans being evaluated, as a value between `1.0` and `100.0`.
    </ResponseField>
    <ResponseField name="last_run" type="datetime">
      The ISO 8601 timestamp of the most recent evaluation cycle.
    </ResponseField>
  </ApiCollapsible>
  <ResponseField name="config" type="object">
    Table column configuration metadata. This describes the structure and display properties of each column in the response, primarily used by the Dashboard UI.
  </ResponseField>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="401" type="Unauthorized">
    The request could not be authenticated. Verify that both `X-Api-Key` and `X-Secret-Key` headers are present and contain valid credentials.
  </ParamField>
  <ParamField name="500" type="Internal Server Error">
    An unexpected server-side error occurred. If this persists, contact support with the request details.
  </ParamField>
</ApiSection>
