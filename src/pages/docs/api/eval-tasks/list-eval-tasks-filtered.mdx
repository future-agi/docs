---
title: "List Eval Tasks with Filtering"
description: "Retrieves a paginated, filterable, and sortable list of eval tasks for a project."
---

# List Eval Tasks with Filtering

Retrieves a paginated, filterable, and sortable list of eval tasks for a project.

<ApiPlayground
  method="GET"
  endpoint="/tracer/eval-task/list_eval_tasks/"
  baseUrl="https://api.futureagi.com"
  parameters={[{"name": "project_id", "in": "query", "required": false, "description": "Filter by project UUID.", "type": "string"}, {"name": "name", "in": "query", "required": false, "description": "Search by task name (case-insensitive partial match).", "type": "string"}, {"name": "page_number", "in": "query", "required": false, "description": "Page number for pagination (default: 0).", "type": "integer"}, {"name": "page_size", "in": "query", "required": false, "description": "Number of results per page (default: 30).", "type": "integer"}]}
/>

## Authentication

All requests require authentication via API keys:

| Header | Description |
|--------|-------------|
| `X-Api-Key` | Your API key |
| `X-Secret-Key` | Your secret key |

## Parameters

### Query Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `project_id` | string (UUID) | No | Filter eval tasks by project. Also accepts `projectId`. |
| `name` | string | No | Search by task name (case-insensitive partial match). |
| `page_number` | integer | No | Page number for pagination. Default: `0`. |
| `page_size` | integer | No | Number of results per page. Default: `30`. |

### Request Body (optional)

Filters and sort parameters can also be passed in the request body:

- **filters**: array of object
  Each filter object contains `column_id`, `operator`, and `value`.
- **sort_params**: array of object
  Each sort object contains `column_id` and `direction` (`"asc"` or `"desc"`).

### Example Filter Body

```json
{
  "filters": [
    {
      "column_id": "status",
      "operator": "equals",
      "value": "pending"
    }
  ],
  "sort_params": [
    {
      "column_id": "created_at",
      "direction": "desc"
    }
  ]
}
```

## Responses

### 200

Successfully retrieved the filtered list of eval tasks.

- **status**: boolean
- **result**: object
  - **metadata**: object — Contains `total_rows` count.
  - **table**: array — Array of eval task objects with `id`, `name`, `status`, `filters_applied`, `created_at`, `evals_applied`, `sampling_rate`, `last_run`.
  - **config**: object — Table column configuration.

### 401

Unauthorized. Authentication credentials were not provided or are invalid.

### 500

Internal Server Error. An unexpected error occurred.

## Code Examples

### cURL

```bash
curl -X GET "https://api.futureagi.com/tracer/eval-task/list_eval_tasks/?project_id=YOUR_PROJECT_ID&page_size=10" \
  -H "X-Api-Key: YOUR_API_KEY" \
  -H "X-Secret-Key: YOUR_SECRET_KEY" \
  -H "Content-Type: application/json"
```

### Python

```python
import requests

url = "https://api.futureagi.com/tracer/eval-task/list_eval_tasks/"
headers = {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
    "Content-Type": "application/json"
}
params = {
    "project_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    "page_size": 10,
    "page_number": 0
}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```

### JavaScript

```javascript
const params = new URLSearchParams({
  project_id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
  page_size: "10",
  page_number: "0"
});

const response = await fetch(`https://api.futureagi.com/tracer/eval-task/list_eval_tasks/?${params}`, {
  method: "GET",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
    "Content-Type": "application/json"
  },
});

const data = await response.json();
console.log(data);
```
