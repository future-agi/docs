---
title: "Unpause Eval Task"
description: "Resumes a previously paused eval task."
---

# Unpause Eval Task

Resumes a previously paused eval task. Only tasks with status `"paused"` can be unpaused. The task status is set back to `"pending"` and evaluation processing resumes from the beginning.

<ApiPlayground
  method="POST"
  endpoint="/tracer/eval-task/unpause_eval_task/"
  baseUrl="https://api.futureagi.com"
  parameters={[{"name": "eval_task_id", "in": "query", "required": true, "description": "UUID of the eval task to resume.", "type": "string"}]}
/>

## Authentication

All requests require authentication via API keys:

| Header | Description |
|--------|-------------|
| `X-Api-Key` | Your API key |
| `X-Secret-Key` | Your secret key |

## Query Parameters

<ParamField body="eval_task_id" type="string" required>
  UUID of the eval task to resume.
</ParamField>

## Response

<ResponseField name="status" type="boolean">Indicates success.</ResponseField>
<ResponseField name="result" type="string">Confirmation message.</ResponseField>

### Responses

| Status | Description |
|--------|-------------|
| `200` | Eval task resumed successfully. |
| `400` | Task is not in `"paused"` state and cannot be unpaused. |
| `401` | Unauthorized. Authentication credentials were not provided or are invalid. |
| `404` | Eval task not found. |

## Code Examples

<CodeGroup>
```python Python
import requests

url = "https://api.futureagi.com/tracer/eval-task/unpause_eval_task/"
headers = {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
}
params = {
    "eval_task_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
}

response = requests.post(url, headers=headers, params=params)
print(response.json())
```
```typescript TypeScript
const params = new URLSearchParams({
  eval_task_id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
});

const response = await fetch(
  `https://api.futureagi.com/tracer/eval-task/unpause_eval_task/?${params}`,
  {
    method: "POST",
    headers: {
      "X-Api-Key": "YOUR_API_KEY",
      "X-Secret-Key": "YOUR_SECRET_KEY",
    },
  }
);

const data = await response.json();
console.log(data);
```
```bash cURL
curl -X POST "https://api.futureagi.com/tracer/eval-task/unpause_eval_task/?eval_task_id=YOUR_EVAL_TASK_ID" \
  -H "X-Api-Key: YOUR_API_KEY" \
  -H "X-Secret-Key: YOUR_SECRET_KEY"
```
</CodeGroup>
