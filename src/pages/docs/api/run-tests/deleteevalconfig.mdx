---
title: "Delete evaluation configuration"
description: "Removes an evaluation configuration from a test run. Cannot delete the last remaining eval config."
---

<ApiPlayground
  method="DELETE"
  endpoint="/simulate/run-tests/{run_test_id}/eval-configs/{eval_config_id}/"
  baseUrl="https://api.futureagi.com"
  parameters={[
    {"name": "run_test_id", "in": "path", "required": true, "description": "UUID of the test run containing the evaluation configuration.", "type": "string"},
    {"name": "eval_config_id", "in": "path", "required": true, "description": "UUID of the evaluation configuration to delete.", "type": "string"}
  ]}
  responseExample={{"message": "Evaluation config deleted successfully"}}
  responseStatus={200}
  responseStatusText="OK"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Path parameters">
  <ParamField path="run_test_id" type="string" required>
    The universally unique identifier (UUID) of the test run containing the evaluation configuration you want to delete. The test run must exist and be accessible within your workspace.
  </ParamField>
  <ParamField path="eval_config_id" type="string" required>
    The universally unique identifier (UUID) of the evaluation configuration to remove from the test run. This operation is irreversible -- once deleted, the configuration and its association with the test run cannot be recovered. Note that the test run must retain at least one evaluation configuration after deletion; attempting to delete the last remaining configuration will result in a `400` error.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={200} statusText="OK">
  <ResponseField name="message" type="string">
    A confirmation message indicating that the evaluation configuration was successfully deleted from the test run (e.g., `"Evaluation config deleted successfully"`).
  </ResponseField>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="400" type="Bad Request">
    The deletion cannot be completed because it would remove the last remaining evaluation configuration from the test run. Every test run must have at least one evaluation configuration at all times. To resolve this, add a new evaluation configuration before deleting the current one, or consider deleting the entire test run instead if no evaluations are needed.
  </ParamField>
  <ParamField name="401" type="Unauthorized">
    The request did not include valid authentication credentials. Ensure both `X-Api-Key` and `X-Secret-Key` headers are present and contain valid, non-expired keys. You can regenerate your API keys from the [Dashboard](https://app.futureagi.com) if needed.
  </ParamField>
  <ParamField name="404" type="Not Found">
    Either the test run with the specified `run_test_id` or the evaluation configuration with the specified `eval_config_id` could not be found. Verify both UUIDs are correct and that the resources have not been deleted previously.
  </ParamField>
  <ParamField name="500" type="Internal Server Error">
    An unexpected error occurred on the server while processing your request. This is typically a transient issue. Retry the request after a brief delay, and if the problem persists, contact Future AGI support with the request details for further investigation.
  </ParamField>
</ApiSection>
