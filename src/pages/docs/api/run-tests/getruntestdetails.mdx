---
title: "Get test run details"
description: "Retrieves detailed information about a specific test run, including associated scenarios, agent definition, and evaluation configurations."
---

<ApiPlayground
  method="GET"
  endpoint="/simulate/run-tests/{run_test_id}/"
  baseUrl="https://api.futureagi.com"
  parameters={[
    {"name": "run_test_id", "in": "path", "required": true, "description": "UUID of the test run to retrieve.", "type": "string"}
  ]}
  responseExample={{"id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890", "name": "Checkout Flow Test", "description": "End-to-end checkout validation", "agent_definition": "b2c3d4e5-f6a7-8901-bcde-f12345678901", "agent_version": "c3d4e5f6-a7b8-9012-cdef-123456789012", "agent_definition_detail": {}, "source_type": "agent_definition", "source_type_display": "Agent Definition", "prompt_template": null, "prompt_template_detail": null, "prompt_version": null, "prompt_version_detail": null, "scenarios": [], "scenarios_detail": [], "dataset_row_ids": [], "simulator_agent": null, "simulator_agent_detail": null, "simulate_eval_configs": [], "simulate_eval_configs_detail": [], "evals_detail": [], "enable_tool_evaluation": false, "created_at": "2026-04-04T12:00:00Z", "updated_at": "2026-04-04T12:00:00Z", "last_run_at": null}}
  responseStatus={200}
  responseStatusText="OK"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Path parameters">
  <ParamField path="run_test_id" type="string" required>
    The UUID of the test run to retrieve. This identifier is returned when you create a test run via the [Create Test Run](/docs/api/run-tests/createruntest) endpoint, or can be found in the results from the [List Test Runs](/docs/api/run-tests/listruntests) endpoint.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={200} statusText="OK">
  <ResponseField name="id" type="string">
    The unique UUID identifier of the test run.
  </ResponseField>
  <ResponseField name="name" type="string">
    The display name of the test run as configured during creation.
  </ResponseField>
  <ResponseField name="description" type="string">
    The description text associated with the test run. Returns an empty string if no description was provided.
  </ResponseField>
  <ResponseField name="agent_definition" type="string or null">
    The UUID of the agent definition associated with this test run. Returns `null` if the test run is configured with a prompt template instead of an agent definition.
  </ResponseField>
  <ResponseField name="agent_version" type="string or null">
    The UUID of the specific agent version used by this test run. Returns `null` if the test run defaults to the active version or if no agent definition is associated.
  </ResponseField>
  <ResponseField name="agent_definition_detail" type="object or null">
    An expanded object containing the full details of the associated agent definition, including its name, system prompt, model configuration, and tool definitions. Returns `null` if no agent definition is associated.
  </ResponseField>
  <ResponseField name="source_type" type="string">
    The source type identifier for this test run. Either `agent_definition` or `prompt`, indicating which type of configuration drives the test.
  </ResponseField>
  <ResponseField name="source_type_display" type="string">
    A human-readable label for the source type, such as "Agent Definition" or "Prompt". Useful for display purposes in user interfaces.
  </ResponseField>
  <ResponseField name="prompt_template" type="string or null">
    The UUID of the prompt template associated with this test run, if applicable. Returns `null` when the test run uses an agent definition.
  </ResponseField>
  <ResponseField name="prompt_template_detail" type="object or null">
    An expanded object containing the full details of the associated prompt template. Returns `null` when no prompt template is associated.
  </ResponseField>
  <ResponseField name="prompt_version" type="string or null">
    The UUID of the specific prompt version used by this test run. Returns `null` when no prompt template is associated or when the default version is used.
  </ResponseField>
  <ResponseField name="prompt_version_detail" type="object or null">
    An expanded object containing the full details of the prompt version, including the prompt text and configuration. Returns `null` when no prompt version is specified.
  </ResponseField>
  <ResponseField name="scenarios" type="array of string">
    An array of scenario UUIDs linked to this test run. These scenarios define the conversation flows the agent will be tested against.
  </ResponseField>
  <ResponseField name="scenarios_detail" type="array of objects">
    An array of expanded scenario objects containing full details for each linked scenario, including name, description, rows, and configuration. Provides all the information needed to understand what the test run covers without requiring additional API calls.
  </ResponseField>
  <ResponseField name="dataset_row_ids" type="array of string">
    An array of specific dataset row UUIDs that this test run is restricted to. An empty array indicates all rows from the associated scenarios are included.
  </ResponseField>
  <ResponseField name="simulator_agent" type="string or null">
    The UUID of the simulator agent used to simulate the user side of conversations during test execution. Returns `null` if no custom simulator agent is configured.
  </ResponseField>
  <ResponseField name="simulator_agent_detail" type="object or null">
    An expanded object containing the full details of the simulator agent, including its name and configuration. Returns `null` if no simulator agent is assigned.
  </ResponseField>
  <ResponseField name="simulate_eval_configs" type="array of string">
    An array of evaluation configuration UUIDs associated with this test run. These configs define the metrics and criteria used to score agent performance.
  </ResponseField>
  <ResponseField name="simulate_eval_configs_detail" type="array of objects">
    An array of expanded evaluation configuration objects containing full details for each associated eval config, including template references, scoring parameters, and field mappings.
  </ResponseField>
  <ResponseField name="evals_detail" type="array of objects">
    An array of evaluation detail objects providing comprehensive information about all evaluations configured for this test run, combining both referenced and inline evaluation configurations.
  </ResponseField>
  <ResponseField name="enable_tool_evaluation" type="boolean">
    Whether tool evaluation is enabled for this test run. When `true`, the evaluation framework assesses tool call correctness during execution.
  </ResponseField>
  <ResponseField name="created_at" type="string">
    ISO 8601 timestamp indicating when the test run was created.
  </ResponseField>
  <ResponseField name="updated_at" type="string">
    ISO 8601 timestamp indicating when the test run was last modified.
  </ResponseField>
  <ResponseField name="last_run_at" type="string or null">
    ISO 8601 timestamp of the most recent execution of this test run. Returns `null` if the test run has never been executed.
  </ResponseField>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="401" type="Unauthorized">
    Authentication credentials were not provided or are invalid. Ensure that both `X-Api-Key` and `X-Secret-Key` headers are included in your request and contain valid, non-expired keys. Verify your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="404" type="Not Found">
    The test run with the specified `run_test_id` could not be found. This can occur if the UUID is incorrect, the test run has been deleted, or the test run belongs to a different organization. Double-check the ID and ensure it matches a test run within your organization.
  </ParamField>
  <ParamField name="500" type="Internal Server Error">
    An unexpected error occurred on the server while processing the request. This is not caused by your input. If this error persists, contact Future AGI support with the request details and timestamp for investigation.
  </ParamField>
</ApiSection>
