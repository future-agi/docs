---
title: "Duplicate persona"
description: "Creates a copy of an existing persona as a new workspace-level persona with a custom name."
---

<ApiPlayground
  method="POST"
  endpoint="/simulate/api/personas/duplicate/{persona_id}/"
  parameters={[
    {"name": "persona_id", "in": "path", "required": true, "description": "UUID of the source persona to duplicate.", "type": "string"}
  ]}
  requestBody={{
    name: "My Custom Persona Copy"
  }}
  responseExample={{
    status: true,
    result: {
      id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
      personaType: "workspace",
      personaTypeDisplay: "Workspace",
      name: "My Custom Persona Copy",
      description: "A cautious customer who asks many questions",
      gender: ["female"],
      ageGroup: ["32-40"],
      occupation: ["Customer Service"],
      location: ["United States"],
      personality: ["Cautious and skeptical"],
      communicationStyle: ["Questioning"],
      multilingual: false,
      languages: [],
      accent: [],
      conversationSpeed: ["1.0"],
      backgroundSound: false,
      finishedSpeakingSensitivity: ["5"],
      interruptSensitivity: ["5"],
      keywords: [],
      metadata: {},
      additionalInstruction: "",
      isDefault: false,
      simulationType: "voice",
      punctuation: "clean",
      slangUsage: "light",
      typosFrequency: "rare",
      regionalMix: "light",
      emojiUsage: "light",
      tone: "casual",
      verbosity: "balanced",
      createdAt: "2026-04-06T14:00:00Z",
      updatedAt: "2026-04-06T14:00:00Z"
    }
  }}
  responseStatus={201}
  responseStatusText="Created"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Path parameters">
  <ParamField path="persona_id" type="string" required>
    The UUID of the source persona to duplicate. This can be either a system-level (prebuilt) persona or a workspace-level (custom) persona. All attributes from the source persona are copied to the new persona except the name, which must be provided in the request body. The new persona is always created as a workspace-level persona regardless of the source persona's type.
  </ParamField>
</ApiSection>

<ApiSection title="Request body">
  <ParamField body="name" type="string" required>
    The name for the duplicated persona. Must be unique within your workspace (case-insensitive comparison). Maximum 255 characters. Choose a descriptive name that distinguishes this copy from the original persona.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={201} statusText="Created">
  <ResponseField name="status" type="boolean">
    Returns `true` when the persona is duplicated successfully.
  </ResponseField>
  <ResponseField name="result" type="object">
    The full persona object of the newly created copy. All attributes are inherited from the source except `name`, `id`, and `personaType` (always `"workspace"`).
  </ResponseField>
  <ApiCollapsible title="Show 30 properties">
    <ResponseField name="id" type="string">
      A new unique identifier (UUID) for the duplicated persona.
    </ResponseField>
    <ResponseField name="personaType" type="string">
      Always `"workspace"` for duplicated personas.
    </ResponseField>
    <ResponseField name="personaTypeDisplay" type="string">
      Human-readable label: `"Workspace"`.
    </ResponseField>
    <ResponseField name="name" type="string">
      The name provided in the request body.
    </ResponseField>
    <ResponseField name="description" type="string or null">
      Inherited from the source persona.
    </ResponseField>
    <ResponseField name="gender" type="array of string">
      Gender attributes inherited from the source.
    </ResponseField>
    <ResponseField name="ageGroup" type="array of string">
      Age group ranges inherited from the source.
    </ResponseField>
    <ResponseField name="occupation" type="array of string">
      Occupation descriptors inherited from the source.
    </ResponseField>
    <ResponseField name="location" type="array of string">
      Geographic locations inherited from the source.
    </ResponseField>
    <ResponseField name="personality" type="array of string">
      Personality traits inherited from the source.
    </ResponseField>
    <ResponseField name="communicationStyle" type="array of string">
      Communication styles inherited from the source.
    </ResponseField>
    <ResponseField name="multilingual" type="boolean">
      Multilingual setting inherited from the source.
    </ResponseField>
    <ResponseField name="languages" type="array of string">
      Languages inherited from the source.
    </ResponseField>
    <ResponseField name="accent" type="array of string">
      Accent options inherited from the source.
    </ResponseField>
    <ResponseField name="conversationSpeed" type="array of string">
      Conversation speed settings inherited from the source.
    </ResponseField>
    <ResponseField name="backgroundSound" type="boolean or null">
      Background sound setting inherited from the source.
    </ResponseField>
    <ResponseField name="finishedSpeakingSensitivity" type="array of string">
      Sensitivity values inherited from the source.
    </ResponseField>
    <ResponseField name="interruptSensitivity" type="array of string">
      Sensitivity values inherited from the source.
    </ResponseField>
    <ResponseField name="keywords" type="array of string">
      Keywords inherited from the source.
    </ResponseField>
    <ResponseField name="metadata" type="object or null">
      Metadata inherited from the source.
    </ResponseField>
    <ResponseField name="additionalInstruction" type="string or null">
      Instructions inherited from the source.
    </ResponseField>
    <ResponseField name="isDefault" type="boolean">
      Always `false` for duplicated personas.
    </ResponseField>
    <ResponseField name="simulationType" type="string">
      Simulation type inherited from the source: `voice` or `text`.
    </ResponseField>
    <ResponseField name="punctuation" type="string or null">
      Punctuation style inherited from the source.
    </ResponseField>
    <ResponseField name="slangUsage" type="string or null">
      Slang usage level inherited from the source.
    </ResponseField>
    <ResponseField name="typosFrequency" type="string or null">
      Typo frequency inherited from the source.
    </ResponseField>
    <ResponseField name="regionalMix" type="string or null">
      Regional mix inherited from the source.
    </ResponseField>
    <ResponseField name="emojiUsage" type="string or null">
      Emoji usage inherited from the source.
    </ResponseField>
    <ResponseField name="tone" type="string or null">
      Tone inherited from the source.
    </ResponseField>
    <ResponseField name="verbosity" type="string or null">
      Verbosity inherited from the source.
    </ResponseField>
    <ResponseField name="createdAt" type="datetime">
      ISO 8601 timestamp of when the copy was created.
    </ResponseField>
    <ResponseField name="updatedAt" type="datetime">
      ISO 8601 timestamp, same as `createdAt` for new copies.
    </ResponseField>
  </ApiCollapsible>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="400" type="Bad Request">
    The request payload is invalid. Common causes include:
    - The `name` field is missing, empty, or contains only whitespace
    - The provided name already exists as a persona in your workspace (case-insensitive)
    - The organization context could not be determined from the request

    Example error responses:
    ```json
    {
      "status": false,
      "result": {
        "name": ["A persona with this name already exists in your workspace."]
      }
    }
    ```
    ```json
    {
      "status": false,
      "result": "Organization context required"
    }
    ```
  </ParamField>
  <ParamField name="401" type="Unauthorized">
    Authentication credentials were not provided or are invalid. Ensure that both `X-Api-Key` and `X-Secret-Key` headers are included in your request and contain valid, non-expired keys. Verify your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="404" type="Not Found">
    The source persona with the specified UUID could not be found. Verify that the `persona_id` is correct and that the source persona has not been deleted. You can retrieve valid persona IDs from the [List personas](/docs/api/personas/listpersonas) endpoint.
  </ParamField>
  <ParamField name="500" type="Internal Server Error">
    An unexpected error occurred on the server while processing the request. This is not caused by your input. If this error persists, contact Future AGI support with the request details and timestamp for investigation.
  </ParamField>
</ApiSection>
