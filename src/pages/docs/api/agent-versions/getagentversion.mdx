---
title: "Get Agent Version"
description: "Retrieves detailed information about a specific version of an agent definition, including the full configuration snapshot captured when the version was created."
---

<ApiPlayground
  method="GET"
  endpoint="/simulate/agent-definitions/{agent_id}/versions/{version_id}/"
  baseUrl="https://api.futureagi.com"
  parameters={[
    {"name": "agent_id", "in": "path", "required": true, "description": "UUID of the agent definition.", "type": "string"},
    {"name": "version_id", "in": "path", "required": true, "description": "UUID of the agent version.", "type": "string"}
  ]}
  responseExample={{
    id: "f7a8b9c0-d1e2-3456-789a-bcdef0123456",
    version_number: 2,
    version_name: "v2",
    status: "active",
    score: 8.5,
    test_count: 12,
    pass_rate: 91.7,
    description: "Improved refund handling",
    commit_message: "Updated refund handling logic",
    is_active: true,
    is_latest: true,
    configuration_snapshot: { agent_name: "support-bot-v2", agent_type: "voice", provider: "vapi", language: "en" },
    created_at: "2026-03-15T10:30:00Z"
  }}
  responseStatus={200}
  responseStatusText="OK"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Path parameters">
  <ParamField path="agent_id" type="string" required>
    The unique identifier (UUID) of the agent definition that owns this version.
  </ParamField>
  <ParamField path="version_id" type="string" required>
    The unique identifier (UUID) of the specific agent version to retrieve. You can obtain version IDs from the [List Agent Versions](/docs/api/agent-versions/listagentversions) endpoint.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={200} statusText="OK">
  <ResponseField name="id" type="string">
    The unique identifier (UUID) of the agent version.
  </ResponseField>
  <ResponseField name="version_number" type="integer">
    The sequential version number, starting from 1. Each new version increments this counter.
  </ResponseField>
  <ResponseField name="version_name" type="string">
    The version name in short format, e.g., `v1`, `v2`, `v3`.
  </ResponseField>
  <ResponseField name="version_name_display" type="string">
    A formatted display name for the version, suitable for UI rendering.
  </ResponseField>
  <ResponseField name="status" type="string">
    The lifecycle status of this version:
    - `draft` — The version is newly created and has not been activated.
    - `active` — The version is currently active and in use.
    - `archived` — The version has been archived and is no longer in active use.
    - `deprecated` — The version is deprecated and should not be used.
  </ResponseField>
  <ResponseField name="status_display" type="string">
    A human-readable display label for the version status.
  </ResponseField>
  <ResponseField name="score" type="number">
    The evaluation score for this version, on a scale from `0.0` to `10.0`. Calculated from test execution results. `null` if no tests have been run.
  </ResponseField>
  <ResponseField name="test_count" type="integer">
    The total number of test executions that have been run against this version.
  </ResponseField>
  <ResponseField name="pass_rate" type="number">
    The percentage of tests that passed, from `0` to `100`. `null` if no tests have been run.
  </ResponseField>
  <ResponseField name="description" type="string">
    The description provided when this version was created.
  </ResponseField>
  <ResponseField name="commit_message" type="string">
    The commit message describing the changes in this version.
  </ResponseField>
  <ResponseField name="release_notes" type="string">
    Optional release notes for this version. `null` if not provided.
  </ResponseField>
  <ResponseField name="agent_definition" type="string">
    The UUID of the parent agent definition.
  </ResponseField>
  <ResponseField name="organization" type="string">
    The UUID of the organization that owns this version.
  </ResponseField>
  <ResponseField name="configuration_snapshot" type="object">
    A complete, immutable snapshot of the agent's configuration at the time this version was created, preserved for reproducibility.
  </ResponseField>
  <ApiCollapsible title="Show 16 properties">
    <ResponseField name="agent_name" type="string">
      The display name of the agent at the time of the snapshot.
    </ResponseField>
    <ResponseField name="agent_type" type="string">
      The type of agent -- either `voice` or `text`.
    </ResponseField>
    <ResponseField name="inbound" type="boolean">
      Whether the agent handled inbound calls at the time of the snapshot.
    </ResponseField>
    <ResponseField name="languages" type="array">
      The list of supported language codes at the time of the snapshot.
    </ResponseField>
    <ResponseField name="language" type="string">
      The primary language as an ISO 639-1 code (e.g., `en`, `es`).
    </ResponseField>
    <ResponseField name="provider" type="string">
      The voice provider powering this agent (`vapi`, `retell`, `eleven_labs`, `livekit`, or `others`).
    </ResponseField>
    <ResponseField name="contact_number" type="string">
      The phone number associated with the agent at the time of the snapshot.
    </ResponseField>
    <ResponseField name="assistant_id" type="string">
      The external assistant identifier from the voice provider.
    </ResponseField>
    <ResponseField name="api_key" type="string">
      The API key configured for the agent's external provider integration.
    </ResponseField>
    <ResponseField name="authentication_method" type="string">
      The authentication method used for provider communication.
    </ResponseField>
    <ResponseField name="observability_enabled" type="boolean">
      Whether observability/tracing was enabled for this agent.
    </ResponseField>
    <ResponseField name="description" type="string">
      The description of the agent at the time of the snapshot.
    </ResponseField>
    <ResponseField name="knowledge_base" type="string">
      The UUID of the linked knowledge base, if any.
    </ResponseField>
    <ResponseField name="commit_message" type="string">
      The commit message describing the changes in this version.
    </ResponseField>
    <ResponseField name="model" type="string">
      The AI model identifier used by this agent.
    </ResponseField>
    <ResponseField name="model_details" type="object">
      Extended details about the model configuration.
    </ResponseField>
  </ApiCollapsible>
  <ResponseField name="is_active" type="boolean">
    Whether this version is the currently active version for the agent definition.
  </ResponseField>
  <ResponseField name="is_latest" type="boolean">
    Whether this version is the most recently created version for the agent definition.
  </ResponseField>
  <ResponseField name="created_at" type="datetime">
    The ISO 8601 timestamp of when this version was created.
  </ResponseField>
  <ResponseField name="updated_at" type="datetime">
    The ISO 8601 timestamp of the most recent modification to this version.
  </ResponseField>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="401" type="Unauthorized">
    The request could not be authenticated. Verify that both `X-Api-Key` and `X-Secret-Key` headers are present and contain valid credentials.
  </ParamField>
  <ParamField name="404" type="Not Found">
    The agent definition or version could not be found. Verify both the `agent_id` and `version_id` are correct.
  </ParamField>
  <ParamField name="500" type="Internal Server Error">
    An unexpected server-side error occurred. If this persists, contact support with the request details.
  </ParamField>
</ApiSection>
