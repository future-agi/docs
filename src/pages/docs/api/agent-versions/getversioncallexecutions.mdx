---
title: "Get call executions for agent version"
description: "Retrieves a paginated list of completed call executions with evaluation results for a specific agent version."
---

# Get call executions for agent version

Retrieves a paginated list of completed call executions that have evaluation results for a specific agent version. Only returns calls with status `completed` and non-empty evaluation outputs.

<ApiPlayground
  method="GET"
  endpoint="/simulate/agent-definitions/{agent_id}/versions/{version_id}/call-executions/"
  baseUrl="https://api.futureagi.com"
  parameters={[{"name": "agent_id", "in": "path", "required": true, "description": "A UUID string identifying the agent definition.", "type": "string"}, {"name": "version_id", "in": "path", "required": true, "description": "A UUID string identifying the agent version.", "type": "string"}, {"name": "limit", "in": "query", "required": false, "description": "Number of items per page.", "type": "integer"}, {"name": "page", "in": "query", "required": false, "description": "Page number.", "type": "integer"}]}
/>

## Authentication

All requests require authentication via API keys:

| Header | Description |
|--------|-------------|
| `X-Api-Key` | Your API key |
| `X-Secret-Key` | Your secret key |

## Parameters

### Path Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `agent_id` | string (UUID) | Yes | The UUID of the agent definition. |
| `version_id` | string (UUID) | Yes | The UUID of the agent version. |

### Query Parameters

| Parameter | Type | Required | Default | Description |
|-----------|------|----------|---------|-------------|
| `limit` | integer | No | 10 | Number of items per page. |
| `page` | integer | No | 1 | Page number. |

## Responses

### 200

A paginated list of call executions.

- **count**: integer
- **next**: string or null
- **previous**: string or null
- **results**: array of objects
  Each object contains:
  - **id**: string (UUID)
  - **service_provider_call_id**: string or null
  - **session_id**: string or null
  - **status**: string
  - **duration**: number or null
  - **start_time**: string (datetime) or null
  - **transcript**: array of objects
  - **scenario**: string
  - **overall_score**: number or null
  - **eval_outputs**: object
  - **eval_metrics**: object or null
  - **scenario_columns**: array
  - **customer_name**: string or null
  - **call_summary**: string or null
  - **ended_reason**: string or null
  - **created_at**: string (datetime)

### 401

Unauthorized. Authentication credentials were not provided or are invalid.

### 404

Not Found. The agent definition or version could not be found.

### 500

Internal Server Error. An unexpected error occurred.

## Code Examples

### cURL

```bash
curl -X GET "https://api.futureagi.com/simulate/agent-definitions/{agent_id}/versions/{version_id}/call-executions/?limit=10&page=1" \
  -H "X-Api-Key: YOUR_API_KEY" \
  -H "X-Secret-Key: YOUR_SECRET_KEY"
```

### Python

```python
import requests

url = "https://api.futureagi.com/simulate/agent-definitions/{agent_id}/versions/{version_id}/call-executions/"
headers = {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY"
}
params = {
    "limit": 10,
    "page": 1
}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```

### JavaScript

```javascript
const params = new URLSearchParams({ limit: "10", page: "1" });
const response = await fetch(`https://api.futureagi.com/simulate/agent-definitions/{agent_id}/versions/{version_id}/call-executions/?${params}`, {
  method: "GET",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY"
  }
});

const data = await response.json();
console.log(data);
```
