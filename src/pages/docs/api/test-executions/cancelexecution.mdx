---
title: "Cancel test execution"
description: "Cancels an active test execution. The execution transitions to cancelling status and then to cancelled once fully stopped."
---

<ApiPlayground
  method="POST"
  endpoint="/simulate/test-executions/{test_execution_id}/cancel/"
  baseUrl="https://api.futureagi.com"
  parameters={[
    {"name": "test_execution_id", "in": "path", "required": true, "description": "UUID of the test execution to cancel.", "type": "string"}
  ]}
  requestBody={{}}
  responseExample={{
    success: true,
    message: "Test execution cancellation initiated",
    test_execution_id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
  }}
  responseStatus={200}
  responseStatusText="OK"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Path parameters">
  <ParamField path="test_execution_id" type="string" required>
    The unique identifier (UUID) of the test execution to cancel. The test execution must be in an active state (`pending`, `running`, or `evaluating`) for the cancellation to be accepted. You can obtain this ID from the [Get test execution details](/docs/api/test-executions/gettestexecutiondetails) or test execution list endpoints.
  </ParamField>
</ApiSection>

<ApiSection title="Request body">
  <ParamField body="(empty)" type="object" optional>
    No request body fields are required. Send an empty JSON object `{}` with a `Content-Type: application/json` header. The cancellation is triggered solely by the path parameter identifying the target execution.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={200} statusText="OK">
  <ResponseField name="success" type="boolean">
    Indicates whether the cancellation request was accepted. Returns `true` when the system has successfully initiated the cancellation process. Note that the test execution may take some time to fully stop all running calls.
  </ResponseField>
  <ResponseField name="message" type="string">
    A human-readable message confirming the cancellation was initiated. Typically returns `"Test execution cancellation initiated"`.
  </ResponseField>
  <ResponseField name="test_execution_id" type="string">
    The UUID of the test execution that was cancelled, echoed back for confirmation and reference.
  </ResponseField>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="400" type="Bad Request">
    The test execution could not be cancelled. This typically occurs when the execution is already in a terminal state (`completed`, `failed`, or `cancelled`) and cannot be cancelled again. Verify the current status of the test execution before attempting cancellation.
  </ParamField>
  <ParamField name="401" type="Unauthorized">
    The request could not be authenticated. Verify that both `X-Api-Key` and `X-Secret-Key` headers are present and contain valid, non-expired credentials. Ensure the API key has access to the workspace that owns this test execution.
  </ParamField>
  <ParamField name="404" type="Not Found">
    No test execution was found with the specified ID. The test execution may have been deleted, or the UUID may be incorrect. Verify the `test_execution_id` value and ensure it belongs to your organization.
  </ParamField>
  <ParamField name="500" type="Internal Server Error">
    An unexpected server-side error occurred while processing the cancellation. The response body may contain an `error` field with additional details. If this persists, contact support with the request details and the `test_execution_id`.
  </ParamField>
</ApiSection>
