---
title: "Get Custom Eval Config"
description: "Retrieves detailed information about a specific custom eval config, including its template association, field mapping, configuration, and evaluation model settings."
---

<ApiPlayground
  method="GET"
  endpoint="/tracer/custom-eval-config/{id}/"
  baseUrl="https://api.futureagi.com"
  parameters={[{"name": "id", "in": "path", "required": true, "description": "UUID of the custom eval config.", "type": "string"}]}
  responseExample={{
    id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
    eval_template: "d4e5f6a7-b8c9-0123-def0-123456789abc",
    name: "my-eval-config",
    project: "f7a8b9c0-d1e2-3456-789a-bcdef0123456",
    config: {},
    mapping: { input: "user_query", output: "assistant_response" },
    error_localizer: false,
    model: "turing_large"
  }}
  responseStatus={200}
  responseStatusText="OK"
/>

<ApiSection title="Authentication">
  <ParamField name="X-Api-Key" type="string" required>
    Your Future AGI API key used to authenticate requests. You can find and manage your API keys in the [Dashboard](https://app.futureagi.com) under Settings.
  </ParamField>
  <ParamField name="X-Secret-Key" type="string" required>
    Your Future AGI secret key, used alongside the API key for request authentication. This is generated when you create an API key in the [Dashboard](https://app.futureagi.com).
  </ParamField>
</ApiSection>

<ApiSection title="Path parameters">
  <ParamField path="id" type="string" required>
    The unique identifier (UUID) of the custom eval config to retrieve. You can obtain this ID from the response of the [Create Custom Eval Config](/docs/api/custom-eval-configs/create-custom-eval-config) or [List Custom Eval Configs](/docs/api/custom-eval-configs/list-configs-filtered) endpoints.
  </ParamField>
</ApiSection>

<ApiSection title="Response" status={200} statusText="OK">
  <ResponseField name="id" type="string">
    The unique identifier (UUID) of the custom eval config.
  </ResponseField>
  <ResponseField name="eval_template" type="string">
    The UUID of the eval template this config is based on. The template defines the evaluation logic, expected inputs, and scoring criteria.
  </ResponseField>
  <ResponseField name="name" type="string">
    The human-readable name assigned to this config when it was created.
  </ResponseField>
  <ResponseField name="config" type="object">
    The configuration dictionary that customizes the eval template's behavior for this specific config. Values are normalized against the template's config schema.
  </ResponseField>
  <ResponseField name="mapping" type="object">
    The field mapping that defines how trace span fields correspond to the eval template's expected inputs (e.g., `input`, `output`, `context`).
  </ResponseField>
  <ResponseField name="project" type="string">
    The UUID of the project this config is associated with.
  </ResponseField>
  <ResponseField name="filters" type="object">
    Any filter criteria applied to this config, which further restrict which spans it evaluates.
  </ResponseField>
  <ResponseField name="error_localizer" type="boolean">
    Whether error localization is enabled for this config. When `true`, the evaluator identifies the specific part of the output that caused a failure.
  </ResponseField>
  <ResponseField name="kb_id" type="string">
    The UUID of the knowledge base file referenced by this config, if any. Used for evaluations that compare output against reference documents.
  </ResponseField>
  <ResponseField name="model" type="string">
    The evaluation model used for scoring, if explicitly set. When `null`, the system default model is used.
  </ResponseField>
  <ResponseField name="eval_group" type="string">
    The name of the eval group this config belongs to, if it has been added to one. Eval groups allow you to organize and apply multiple configs together.
  </ResponseField>
</ApiSection>

<ApiSection title="Errors">
  <ParamField name="401" type="Unauthorized">
    The request could not be authenticated. Verify that both `X-Api-Key` and `X-Secret-Key` headers are present and contain valid credentials.
  </ParamField>
  <ParamField name="404" type="Not Found">
    No custom eval config was found with the specified ID. The config may have been deleted, or the ID may be incorrect.
  </ParamField>
</ApiSection>
