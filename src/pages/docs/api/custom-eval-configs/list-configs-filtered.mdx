---
title: "List Custom Eval Configs"
description: "Retrieves a filtered list of custom eval configs, optionally scoped to a project or eval task."
---

# List Custom Eval Configs

Retrieves a filtered list of custom eval configs, optionally scoped to a specific project or eval task.

<ApiPlayground
  method="GET"
  endpoint="/tracer/custom-eval-config/list_custom_eval_configs/"
  baseUrl="https://api.futureagi.com"
  parameters={[
    {"name": "project_id", "in": "query", "required": false, "description": "Filter by project UUID.", "type": "string"},
    {"name": "task_id", "in": "query", "required": false, "description": "Filter by eval task UUID.", "type": "string"}
  ]}
/>

## Authentication

All requests require authentication via API keys:

| Header | Description |
|--------|-------------|
| `X-Api-Key` | Your API key |
| `X-Secret-Key` | Your secret key |

## Query Parameters

<ParamField body="project_id" type="string">
  Filter configs by project UUID. Also accepts `projectId`.
</ParamField>

<ParamField body="task_id" type="string">
  Filter configs by eval task UUID. Returns only configs associated with the specified eval task. Also accepts `taskId`.
</ParamField>

<ParamField body="filters" type="string">
  JSON-encoded dictionary of additional filters. Must be a valid dictionary if provided.
</ParamField>

## Response

Returns an array of custom eval config objects.

<ResponseField name="id" type="string">UUID of the custom eval config.</ResponseField>
<ResponseField name="eval_template" type="string">UUID of the associated eval template.</ResponseField>
<ResponseField name="name" type="string">Config name.</ResponseField>
<ResponseField name="config" type="object">Configuration dictionary.</ResponseField>
<ResponseField name="mapping" type="object">Field mapping configuration.</ResponseField>
<ResponseField name="project" type="string">UUID of the associated project.</ResponseField>
<ResponseField name="filters" type="object">Applied filter criteria.</ResponseField>
<ResponseField name="error_localizer" type="boolean">Whether error localization is enabled.</ResponseField>
<ResponseField name="kb_id" type="string">UUID of the associated knowledge base file, if any.</ResponseField>
<ResponseField name="model" type="string">Model used for evaluation, if set.</ResponseField>
<ResponseField name="eval_group" type="string">Name of the associated eval group, if any.</ResponseField>

### Responses

| Status | Description |
|--------|-------------|
| `200` | Successfully retrieved the filtered list. |
| `400` | Invalid `filters` format — must be a dictionary. |
| `401` | Unauthorized. Authentication credentials were not provided or are invalid. |
| `500` | Internal Server Error. |

## Code Examples

<CodeGroup>
```python Python
import requests

url = "https://api.futureagi.com/tracer/custom-eval-config/list_custom_eval_configs/"
headers = {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
}
params = {
    "project_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```
```typescript TypeScript
const params = new URLSearchParams({
  project_id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890",
});

const response = await fetch(
  `https://api.futureagi.com/tracer/custom-eval-config/list_custom_eval_configs/?${params}`,
  {
    method: "GET",
    headers: {
      "X-Api-Key": "YOUR_API_KEY",
      "X-Secret-Key": "YOUR_SECRET_KEY",
    },
  }
);

const data = await response.json();
console.log(data);
```
```bash cURL
curl -X GET "https://api.futureagi.com/tracer/custom-eval-config/list_custom_eval_configs/?project_id=YOUR_PROJECT_ID" \
  -H "X-Api-Key: YOUR_API_KEY" \
  -H "X-Secret-Key: YOUR_SECRET_KEY"
```
</CodeGroup>
