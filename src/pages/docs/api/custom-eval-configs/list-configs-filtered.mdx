---
title: "List Custom Eval Configs with Filtering"
description: "Retrieves a filtered list of custom eval configs, optionally scoped to a project or eval task."
---

# List Custom Eval Configs with Filtering

Retrieves a filtered list of custom eval configs, optionally scoped to a specific project or eval task.

<ApiPlayground
  method="GET"
  endpoint="/tracer/custom-eval-config/list_custom_eval_configs/"
  baseUrl="https://api.futureagi.com"
  parameters={[{"name": "project_id", "in": "query", "required": false, "description": "Filter by project UUID.", "type": "string"}, {"name": "task_id", "in": "query", "required": false, "description": "Filter by eval task UUID to get configs associated with that task.", "type": "string"}]}
/>

## Authentication

All requests require authentication via API keys:

| Header | Description |
|--------|-------------|
| `X-Api-Key` | Your API key |
| `X-Secret-Key` | Your secret key |

## Parameters

### Query Parameters

| Parameter | Type | Required | Description |
|-----------|------|----------|-------------|
| `project_id` | string (UUID) | No | Filter configs by project. Also accepts `projectId`. |
| `task_id` | string (UUID) | No | Filter configs by eval task. Also accepts `taskId`. Returns only configs associated with the specified eval task. |
| `filters` | string (JSON) | No | JSON-encoded dictionary of additional filters. Must be a valid dictionary if provided. |

## Responses

### 200

Successfully retrieved the filtered list of custom eval configs.

- **status**: string
- **result**: array
  Array of custom eval config objects, each containing `id`, `eval_template`, `name`, `config`, `mapping`, `project`, `filters`, `error_localizer`, `kb_id`, `model`, `eval_group`.

### 400

Bad Request. Invalid filters format — must be a dictionary.

### 401

Unauthorized. Authentication credentials were not provided or are invalid.

### 500

Internal Server Error. An unexpected error occurred.

## Code Examples

### cURL

```bash
curl -X GET "https://api.futureagi.com/tracer/custom-eval-config/list_custom_eval_configs/?project_id=YOUR_PROJECT_ID" \
  -H "X-Api-Key: YOUR_API_KEY" \
  -H "X-Secret-Key: YOUR_SECRET_KEY" \
  -H "Content-Type: application/json"
```

### Python

```python
import requests

url = "https://api.futureagi.com/tracer/custom-eval-config/list_custom_eval_configs/"
headers = {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
    "Content-Type": "application/json"
}
params = {
    "project_id": "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
}

response = requests.get(url, headers=headers, params=params)
print(response.json())
```

### JavaScript

```javascript
const params = new URLSearchParams({
  project_id: "a1b2c3d4-e5f6-7890-abcd-ef1234567890"
});

const response = await fetch(`https://api.futureagi.com/tracer/custom-eval-config/list_custom_eval_configs/?${params}`, {
  method: "GET",
  headers: {
    "X-Api-Key": "YOUR_API_KEY",
    "X-Secret-Key": "YOUR_SECRET_KEY",
    "Content-Type": "application/json"
  },
});

const data = await response.json();
console.log(data);
```
