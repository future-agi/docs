---
title: "Roles & Permissions"
description: "Deciding who can reach what across an organization and its workspaces"
---

<TLDR>
- **Organization:** manages people, billing, and platform API keys
- **Workspace:** contains the actual work
- **Your access:** you have a role in the organization and a role in each workspace you belong to
- **Owners and Admins:** reach every workspace. Everyone else gets what their workspace role allows
</TLDR>

## Set up your team

If you are the first person in your organization, this is the whole job. It is five steps, all in the dashboard, and it takes about fifteen minutes. The rest of this page is the reference you come back to.

<Steps>
  <Step title="Decide how many workspaces you need">
    A workspace is the data boundary. Datasets, prompts, evaluations, annotation queues and knowledge bases live inside one and are invisible from the others.

    One workspace is fine if everyone is allowed to see everything. Add a second when a group must not see another group's work, or when you want production and staging kept apart.

    To add one, open the switcher at the top of the sidebar, hover the workspace name, and choose **Create Workspace**. Or go to **Settings > Workspaces** and click **Create New Workspace**. Name it and click **Create workspace**. You become its admin.

    Names have to be unique in the organization. Reusing one gives you **Workspace with this name already exists**.
  </Step>

  <Step title="Pick a role for each person">
    Two roles per person: one in the organization, one in each workspace they belong to. Match them to the job rather than the title.

    | The person | Organization role | Workspace role |
    |---|---|---|
    | You, setting this up | Owner | Reaches every workspace automatically |
    | Runs the account with you: people, billing, keys | Admin | Reaches every workspace automatically |
    | Team lead who owns one workspace and manages its people | Member | Workspace Admin |
    | Engineer or analyst doing the work | Member | Workspace Member |
    | Stakeholder who should only read | Viewer | Workspace Viewer |

    <Warning>
    **Owners and Admins reach every workspace, automatically and permanently.** You cannot give someone Admin and keep a workspace away from them. If a boundary matters, make them a Member or Viewer and add them only to the workspaces they should see.
    </Warning>

    To give someone control of one workspace and nothing else, make them an organization **Member** and a **Workspace Admin** in that workspace. They can then invite into it and manage its members, without reaching billing, organization settings, or anyone else's workspace.
  </Step>

  <Step title="Invite them">
    Go to **Settings > Members** and click **Invite User**.

    Enter the addresses in **Emails**, comma separated, so one invite can cover several people. Pick the **Organization Role**, then the **Workspace Role** and the **Workspaces** it applies to. Click **Send Invite**.

    Choosing Admin or Owner removes both workspace fields and says *Owner and Admin roles have automatic access to all workspaces.* That is expected.

    Each person appears as **Pending** until they accept, or straight away as **Active** if they already have a Future AGI account. An invite lasts 7 days, and a Pending row carries **Resend the invite** and **Cancel invite**.
  </Step>

  <Step title="Hand out the API keys">
    Your SDK and your traces authenticate with a platform API key. Open **Build > Keys** in the left sidebar and click **Add API Key**. It asks for a name and nothing else.

    You are then shown an API Key and a Secret Key. Both stay masked, so take each with its copy button and store them before you close the dialog. It does not come back.

    Only Owners, Admins and Workspace Admins see this page at all, so decide who is issuing keys before you go further.
  </Step>

  <Step title="Check it worked">
    Three things to look at, all read-only:

    - **Settings > Members** lists everyone with their role, their workspaces, and Active or Pending
    - **Settings > Workspaces** lists every workspace with its admins
    - Ask one teammate to sign in. They should see their own workspaces in the switcher and nothing else
  </Step>
</Steps>

<Note>
**You are done when** every person is on the Members page with the role you intended, and a Member or Viewer signing in sees only the workspaces you put them in.
</Note>

### When someone says they cannot see something

Work down this list before raising it with anyone.

| What they say | What it usually is |
|---|---|
| "I cannot see the workspace at all" | They were never added to it. Only Owners and Admins get every workspace. Add them from **Settings > Members** |
| "I can see it but I cannot create anything" | They are a Workspace Viewer there. Every action on the page will be greyed out. Change their workspace role |
| "I cannot see your dataset or your evaluation" | Those live in the workspace they were made in. Add the person to that workspace, or rebuild it in one you share |
| "That Settings page is missing for me" | It is not in their role's navigation. That is the whole answer, and typing the address will not help |
| "I clicked it and nothing happened" | Almost always a permission, not a bug. Compare their role against the tables below |

## The two levels

Every Future AGI account has an organization. Inside it are one or more workspaces.

<div style={{ display: 'flex', justifyContent: 'center', margin: '1.5rem 0' }}>
  <img src="/images/docs/rbac/access-levels.svg" alt="Access has two levels. The organization holds members and roles, the plan and billing, and organization settings. Under it sit workspaces, and every workspace holds the same kinds of thing: traces, datasets, evaluations, prompts, annotation queues and knowledge bases." style={{ border: 'none', width: '100%', maxWidth: '660px', margin: '1.75rem auto', display: 'block' }} />
</div>

*The organization is the billing boundary. The workspace is the data boundary.*

The organization is where billing lives. The workspace is where the data lives. Each has its own set of roles, so you hold one role in the organization and one in each workspace you belong to.

Every organization has one default [workspace](/docs/admin-settings/workspace-management). Workspace names are unique inside an organization, and reusing one gives you **Workspace with this name already exists**.

## Organization roles

Everyone in an organization holds one of four roles. They rank in this order, Owner highest and Viewer lowest. The ranking decides who can invite and manage whom.

These are the descriptions the product itself shows you when you set someone's role.

<CardGroup cols={2}>
  <Card title="Owner" icon="shield">
    Full control. Can manage billing, members, and transfer ownership

    An organization keeps at least one Owner at all times
  </Card>
  <Card title="Admin" icon="gear">
    Full control on manage settings, members, and permissions

    Cannot change an Owner or another Admin
  </Card>
  <Card title="Member" icon="users">
    Collaborate within the space, accessing tools and tasks shared with them

    Cannot open organization settings
  </Card>
  <Card title="Viewer" icon="eye">
    Read-only access to shared resources

    Cannot open organization settings
  </Card>
</CardGroup>

<Note>
Owners and Admins are **Workspace Admins** in every workspace, automatically.

- They need no workspace membership of their own
- Promoting someone to Admin or Owner grants that access across every workspace at once
- Demoting them asks you to pick the workspaces they keep
</Note>

## Workspace roles

A Member or Viewer sees nothing in a workspace until they are added to it. Their role in that workspace is one of three.

| Role | View | Create and edit | Manage workspace members |
|---|---|---|---|
| Workspace Admin | Yes | Yes | Yes |
| Workspace Member | Yes | Yes | No |
| Workspace Viewer | Yes | No | No |

Everyone needs at least one workspace. If someone has none, they are added to the default workspace when they first sign in.

## When both roles apply

You hold an organization role and, in each workspace you belong to, a workspace role.

- Owner or Admin: Workspace Admin in every workspace, whether or not they were added to it
- Everyone else: the workspace role applies inside that workspace
- Member or Viewer with no membership: no access, and the workspace does not appear for them

Inside a workspace, the workspace role wins, in both directions. An organization Viewer added as Workspace Member can create, edit, and share there. An organization Member added as Workspace Viewer cannot: Add Dataset, Add Row, Add Column, Run Prompt, Evaluate, Share, and Create prompt are all greyed out for them.

One person can hold a different role in each workspace, and it shows. In a workspace where you are a Workspace Member, a dataset gives you Add Row, Add Column, Run Prompt, Evaluate, and Share. In a workspace where you are a Workspace Viewer, the same dataset greys all of those out and leaves you Compare and the read-only tabs.

Being a Workspace Admin does not let you open organization settings or create a workspace. Those need an organization Admin or Owner. What it does give you is **Invite members** in the workspace switcher, the Members tab inside that workspace's own settings, and the **Build > Keys** page.

## What each role actually sees

Left sidebar and Settings are the two places a role difference is visible immediately. Both are the same in every workspace you switch to, because they follow your organization role, not the workspace.

### Settings

Open Settings from the workspace switcher at the top of the sidebar. It is a grouped tree, not a flat list, and this list follows your **organization** role only.

| Settings page | Owner | Admin | Member | Viewer |
|---|---|---|---|---|
| Profile | Yes | Yes | Yes | Yes |
| Usage Summary | Yes | Yes | Yes | Yes |
| AI Providers | Yes | Yes | Yes | No |
| Members | Yes | Yes | No | No |
| Integrations | Yes | Yes | No | No |
| Falcon AI Connectors | Yes | Yes | No | No |
| Plans & Pricing | Yes | Yes | No | No |
| Billing | Yes | Yes | No | No |
| MCP Server | Yes | Yes | No | No |
| Org Settings | Yes | Yes | No | No |
| Workspaces | Yes | Yes | No | No |

Below those sits **Your Workspaces**, which lists every workspace you belong to and jumps you into that workspace's own settings. Owners and Admins see every workspace in the organization there.

Being a Workspace Admin adds nothing to this list. A Workspace Admin has no organization **Members** entry, and reaches their own workspace's member list through **Your Workspaces** or through **Workspace settings** in the switcher. What the workspace role does change is the tabs you get once you are in there: General, Members, Integrations, and AI Providers for a Workspace Admin, General only for a Workspace Member, and General with no Save button for a Workspace Viewer.

### The left sidebar

Everyone sees the same twenty items, with one exception: **Build > Keys** appears for Owners, Admins, and Workspace Admins, and for nobody else. That one does follow the workspace role, as does **Invite members** in the switcher.

<Warning>
Go by your left navigation. If a Settings page is not listed there, your role is not meant to reach it, and typing its address never explains itself. Depending on the page you get a 404, or a silent return to Datasets, or occasionally the page itself with buttons that will not do anything for you. None of those mean the feature is missing. Ask an Owner or Admin.
</Warning>

## One person, many organizations

One user can belong to several organizations, with a different role in each. Owner in one, Member in another, Viewer in a third.

Organizations share nothing: separate workspaces, separate members, separate plan, separate billing, separate keys.

Move between them from the switcher at the top of the sidebar. Hover **Organization** to see the list. Future AGI remembers where you were, so the organization and workspace you last used are where you land next time, on any device.

<Warning>
A [platform API key](/docs/admin-settings/api-keys) belongs to one organization. Whatever the SDK sends with that key lands in that organization, no matter which one you have open in the dashboard. If data is arriving in an unexpected organization, check which key sent it.
</Warning>

Which workspace your SDK data lands in is resolved in this order:

<Steps>
  <Step title="The key's own workspace">
    If the key was created against a specific workspace, the data lands there.
  </Step>
  <Step title="An explicit workspace on the request">
    Otherwise the `X-Workspace-Id` header or a `workspace_id` query parameter is used, provided that workspace is in the same organization and you have access to it.
  </Step>
  <Step title="Your current workspace">
    Otherwise the data lands in the workspace you last used in that organization, then the first workspace you belong to, and if you belong to none, the organization's default workspace.
  </Step>
</Steps>

Traces sent to the collector use the key's workspace, or the organization's default workspace. A workspace header on a trace export is ignored.

## What is scoped where

This boundary decides what a teammate in another workspace can see.

<CardGroup cols={2}>
  <Card title="Lives in a workspace" icon="layer-group">
    - Datasets
    - Evaluations you create, templates, and evaluation groups
    - Prompts and prompt folders
    - Annotations and annotation queues
    - Knowledge bases
    - Custom models
    - Agent playground graphs
    - Optimization runs
    - Integration connections
  </Card>
  <Card title="Lives in the organization" icon="database">
    - Members, roles, and invites
    - Plan, billing, invoices, and wallet balance
    - Resource limits and rate limits
    - Platform API keys
    - Gateway API keys and gateway request logs
    - Organization settings
    - MCP server settings
  </Card>
</CardGroup>

Switching workspace changes the work, and only the work. Datasets and prompts are completely separate lists per workspace. Your sidebar and your Settings do not change.

**Future AGI's built-in evaluations are everywhere.** All of them are available in every workspace of every organization. An evaluation template you build yourself belongs to the workspace you built it in. Rebuild it in the second workspace, or keep shared evaluations in a workspace everyone belongs to.

**Gateway traffic does not split by workspace.** Gateway keys belong to the organization, and every gateway request log is stamped with the organization rather than a workspace. Gateway access cannot be narrowed to a single workspace.

<Warning>
**Gateway** is in the sidebar for everyone, including a Viewer, and every page under it opens: Providers, API Keys, Guardrails, Fallbacks, the request logs, budgets, and webhooks. Treat gateway configuration as visible to your whole organization.

Writing is still checked, just later than everywhere else. The gateway leaves its create buttons live rather than greying them out, and refuses on submit with **Write access denied to this workspace**. Everywhere else in the product a button you cannot use is disabled, so this one page will feel different.
</Warning>

## Sharing outside the roles

A share link gives someone access to one thing without giving them a role. Anyone who can create or edit in a workspace can create one. For a Workspace Viewer, the Share button is greyed out.

**Datasets** get one kind of link. The dialog says *Share this link to give others access to the selected dataset. Anyone with the link will be able to view the data.* There is no restricted option.

**Observe projects** get a choice under **Who can access**:

| Option | Who can open it |
|---|---|
| Anyone with the link | No sign-in required to view |
| Restricted | Only people you add can view. This is the default |

A restricted share also has an **Invite people** field, so you can name the email addresses directly. Those people sign in, but need not be in your organization.

<Warning>
An "anyone with the link" share is public. Whoever holds the URL can open the resource with no account, and the link does not expire on its own. Treat that link as the credential.
</Warning>

## API keys

Future AGI issues two kinds of key. They cover different parts of the platform, and different roles can reach them.

<CardGroup cols={2}>
  <Card title="Platform API key" icon="shield">
    The key and secret the SDK uses

    **Scope:** one organization

    **Who can reach it:** Owners, Admins, and Workspace Admins, from **Build > Keys** in the left sidebar. For everyone else the page is not in the sidebar and its address returns a 404
  </Card>
  <Card title="Gateway API key" icon="plug">
    The key used to call models through the gateway

    **Scope:** the whole organization, never a single workspace

    **Who can reach it:** everyone in the organization, from **Gateway > API Keys**. Creating one still needs write access to a workspace
  </Card>
</CardGroup>

Every organization starts with one platform key, `system_org_key`. **Add API Key** asks for a name and nothing else, then shows you the pair. Both the key and the secret stay masked on screen and are taken with the copy button, so store them the moment they are issued.

Each key row carries two actions:

| Action | What it does | Who |
|---|---|---|
| Disable Key | Turns off the key and its secret together. The row gains a **Disabled** badge and the menu changes to **Re-enable key**, so this is reversible | Owner, Admin, Workspace Admin |
| Delete Key | Removes the key and its secret permanently. It cannot be undone | Owner only |

<Warning>
**Delete Key** is offered to everyone who can open the page, but only an Owner's goes through. An Admin or a Workspace Admin gets as far as the confirmation and then **You are not authorized to access this resource**, and the key stays. If a key has to go and you are not an Owner, disable it and ask an Owner to finish the job.
</Warning>

<Tip>
To rotate a key, create the replacement first, update your application, then disable the old key. Disabling is reversible if something still depends on it, and it is the part you can do yourself.
</Tip>

## Inviting people

Owners and Admins invite from [Members](/docs/admin-settings/user-management). A Workspace Admin cannot open that page, and uses **Invite members** in the workspace switcher instead. The role each can grant is different.

| Who is inviting | What they can grant | Which workspaces |
|---|---|---|
| Owner | Any organization role, up to Owner | Any |
| Admin | Admin, Member, or Viewer. Owner is not offered | Any |
| Workspace Admin | A workspace role only | The workspaces they belong to |

You can grant your own role or anything below it, never above. An Owner can invite another Owner and an Admin can invite another Admin. An Admin cannot create an Owner: the Organization Role list simply does not contain it.

A Workspace Admin's invite has no Organization Role field at all. They pick a workspace role, one of Workspace Admin, Workspace Member, or Workspace Viewer.

<Steps>
  <Step title="Open the members page">
    Go to **Settings > Members** and click **Invite User**. A Workspace Admin uses **Invite members** in the workspace switcher instead.
  </Step>
  <Step title="Enter the email addresses">
    One invite can cover several people at once, as a comma separated list.
  </Step>
  <Step title="Pick the organization role">
    Owner, Admin, Member, or Viewer, within the limit above.
  </Step>
  <Step title="Choose workspace access">
    Pick the workspaces, and one workspace role that applies to all of them. Choosing Admin or Owner removes both fields and replaces them with *Owner and Admin roles have automatic access to all workspaces.*
  </Step>
  <Step title="Send">
    An invitation email goes out and the row shows as **Pending**. If the address already has a Future AGI account, they are added straight away and the row shows as **Active**.
  </Step>
</Steps>

<img src="/images/docs/rbac/invite-modal.png" alt="The invite dialog, showing the emails field, the organization role selector, one workspace role, and the workspaces to apply it to." style={{ borderRadius: '5px' }} />

*Granting Admin or Owner removes the workspace fields, because those roles reach every workspace already.*

An invite is valid for 7 days, after which it shows as Expired. The link inside the email expires sooner than the invite itself, so if someone tells you the link no longer works, resend rather than waiting. A **Pending** row carries exactly two actions, **Resend the invite** and **Cancel invite**.

Inviting someone who is already an active member does not create a second invite. It adds that person to the workspaces you selected, and raises their organization role if you picked a higher one.

## Changing and removing people

<img src="/images/docs/rbac/users-list.png" alt="The Members page, with one row per person showing their organization role, their workspaces, their status, and their start date." style={{ borderRadius: '5px' }} />

*Every person carries one organization role and the workspaces they belong to.*

**Settings > Members** lists everyone with their name, organization role, workspaces, email, status, and start date, and filters for status and role. Each row carries two actions, **Edit user info** and **Remove from organization**.

You can act on anyone whose role is lower than yours. Owners are the exception, and can act on other Owners. Where you cannot, the menu is disabled and says why:

- *You can't edit your own role.*
- *You cannot manage a user at or above your own level.*

**Edit user info** holds an Organization Role and a Workspace Role. Setting someone to Member or Viewer adds a required **Workspaces** field, because *Members and Viewers need explicit workspace access.* Whatever you leave unselected there, they lose.

- Promoting someone to Admin or Owner makes them a Workspace Admin in every workspace at once
- Demoting an Admin to Member or Viewer asks you to pick the workspaces they keep
- Removing someone from the organization ends their access and marks them Deactivated. The record is kept, so you can reinstate them later
- Removing someone from a single workspace leaves their organization membership intact
- Nobody can be removed from their last workspace. An Owner or Admin cannot be removed from a workspace, because they reach every workspace regardless
- The last remaining Owner can neither be demoted nor removed. Promote a second Owner first
- You cannot remove yourself, from the organization or from a workspace

Removal does not delete anything a person produced. Annotations, scores, and other output stay in place.

## Workspaces

Owners and Admins create workspaces, from **Create Workspace** in the switcher or from **Settings > Workspaces**. The dialog asks for a name and tells you *You will be the admin of this workspace.* Everyone else has no Create Workspace option at all.

**Settings > Workspaces** lists every workspace with its admins, start date, and last update.

<Note>
There is no way to delete a workspace from the dashboard. Neither the workspace list nor a workspace's own General page offers one. If you need a workspace removed, contact the Future AGI team.
</Note>

## Annotation queue roles

[Annotation queues](/docs/annotations/features/queues) carry one more set of roles, granted per person per queue. They control what someone can do to the items in a queue. They do not control who can see the queue: anyone with access to the workspace can browse it.

| Queue role | What it covers |
|---|---|
| Annotator | Annotates the items assigned to them |
| Reviewer | Reviews other people's annotations and sends work back for rework |
| Manager | Everything a Reviewer does, plus managing the queue itself |

Each person you add to a queue gets three independent checkboxes, one per role, so the roles combine freely. Someone needs both Annotator and Reviewer ticked to do both jobs. Whoever creates the queue is added with all three already ticked.

The people you can add are the members of the workspace the queue is in.

<Note>
Inside a queue, the queue role applies instead of the workspace role. Someone with the Annotator role can annotate in that queue even if they cannot create or edit anything else in the workspace.
</Note>

Two settings on the queue change how work is handed out:

- **Auto-assign items to all annotators.** With this on, every annotator is assigned every item and anyone can annotate any item
- **Submissions per item.** How many annotators must label each item, at most the number you have added

## Plans, billing, and usage

[Billing](/docs/admin-settings/billing-pricing) is handled at the organization. There is one plan, one balance, and one set of limits, and every workspace draws on them. Invoices are issued to the organization, one per billing period. Adding a member or a workspace does not change the plan.

Plans & Pricing and Billing are open to Owners and Admins. **Usage Summary** is open to everyone and breaks consumption down by workspace, which shows where consumption came from without creating a per-workspace budget or a separate bill.

Billing, Plans & Pricing, and Usage Summary are Future AGI Cloud only. Self-hosted deployments get a **License** page instead.

## Organization security

Owners and Admins reach [**Settings > Org Settings**](/docs/admin-settings/organization-settings), titled Organization Settings, which holds the organization name and the security policy.

Under **Organization Security** there is one toggle, **Require 2FA for all members**. With it on, all organization members must set up two-factor authentication.

<Note>
You must enable two-factor authentication on your own account before requiring it for the organization. The page links straight to Profile Settings so you can.
</Note>

Members and Viewers cannot open Org Settings, so the policy is not theirs to change.

## Permission summary

### By organization role

| Action | Owner | Admin | Member | Viewer |
|---|---|---|---|---|
| See the workspaces they belong to | Yes | Yes | Yes | Yes |
| Reach every workspace automatically | Yes | Yes | No | No |
| Create a workspace | Yes | Yes | No | No |
| Invite and manage members | Yes | Yes | No | No |
| Manage Owners and other Admins | Yes | No | No | No |
| Open organization settings | Yes | Yes | No | No |
| Open billing and pricing | Yes | Yes | No | No |
| Open the Keys page | Yes | Yes | No | No |
| Open Usage Summary | Yes | Yes | Yes | Yes |
| Reach the gateway | Yes | Yes | Yes | Yes |

What a Member or Viewer can do inside a workspace comes from their workspace role, below. A Member or Viewer who is a Workspace Admin somewhere can also invite into that workspace and open the Keys page.

### By workspace role

| Workspace action | Workspace Admin | Workspace Member | Workspace Viewer |
|---|---|---|---|
| View the work in the workspace | Yes | Yes | Yes |
| Create and edit | Yes | Yes | No |
| Create a share link | Yes | Yes | No |
| Manage workspace members | Yes | No | No |
| Invite into that workspace | Yes | No | No |
| Open that workspace's Integrations and AI Providers | Yes | No | No |
| Open the Keys page | Yes | No | No |

<Note>
Managing workspace members still respects the organization ranking. A Workspace Admin whose organization role is Member can act on the people in their workspace whose organization role is Viewer, and the rest of the rows are disabled with *You cannot manage a user at or above your own level.* The two actions on a row you can act on are **Edit user info** and **Remove from workspace**.
</Note>

## FAQ

<AccordionGroup>
  <Accordion title="Can the same person be in more than one organization?">
    Yes, with a different role in each. The organizations share nothing, and you move between them from the switcher at the top of the sidebar.
  </Accordion>
  <Accordion title="Can someone hold different roles in different workspaces?">
    Yes. Workspace Admin in one workspace and Workspace Viewer in another is a valid combination, and the difference is visible: the actions on a dataset are live in one and greyed out in the other.
  </Accordion>
  <Accordion title="I have an organization role and a workspace role. Which one applies?">
    Owners and Admins are Workspace Admins everywhere. For everyone else the workspace role decides, in both directions. An organization Viewer added as Workspace Member can create, edit, and share. An organization Member added as Workspace Viewer cannot.
  </Accordion>
  <Accordion title="I clicked Delete Key and nothing happened.">
    Only an Owner can delete a platform API key. Everyone else who can open the page sees the button, gets the confirmation, and is then told **You are not authorized to access this resource**. Disable the key instead, and ask an Owner to delete it.
  </Accordion>
  <Accordion title="I demoted an Admin to Member. Do they still see every workspace?">
    Only the ones you selected during the demotion. Setting someone to Member or Viewer adds a required Workspaces field, and access to every unselected workspace is removed in the same step.
  </Accordion>
  <Accordion title="I am an organization Admin but I was never added to a workspace. Can I see it?">
    Yes. Owners and Admins are Workspace Admins in every workspace. They appear in your workspace switcher without being added.
  </Accordion>
  <Accordion title="Why can a colleague not see the evaluation I built?">
    Evaluations you create belong to the workspace they were created in. Either add your colleague to that workspace, or rebuild the evaluation in one you both belong to. Future AGI's built-in evaluations are always available everywhere.
  </Accordion>
  <Accordion title="A Settings page will not open for me. Is it broken?">
    No. If a page is not listed in your Settings navigation, your role cannot reach it. Typing the address gives you either a 404 or a silent return to Datasets, neither of which explains itself. Ask an Owner or Admin.
  </Accordion>
  <Accordion title="Can I show something to someone outside my organization?">
    Yes. Share the dataset or the Observe project as a link. A link set to anyone with the link needs no account at all. A restricted link needs the people you name to sign in, but not to join your organization.
  </Accordion>
  <Accordion title="Do you support SSO?">
    Yes. SAML, along with Google and GitHub sign in, and passkeys. Talk to the Future AGI team before setting SAML up. Once it is configured, password resets are turned off and new sign ups have to come through SAML. Existing password and social logins keep working.
  </Accordion>
  <Accordion title="Can I give one team its own gateway logs?">
    No. Gateway keys and gateway request logs are held at the organization, so gateway traffic is not partitioned by workspace. The gateway also has no role checks, so anyone who can sign in can reach it.
  </Accordion>
  <Accordion title="My SDK data is going to the wrong place. What should I check?">
    The key. A platform API key is bound to one organization. Selecting a different organization in the dashboard does not change where the key writes.
  </Accordion>
  <Accordion title="Can a Workspace Admin invite an Admin?">
    No. Their invite carries no Organization Role field, so they grant a workspace role only. Granting an organization role needs an Admin or an Owner.
  </Accordion>
  <Accordion title="Can I bill each workspace separately?">
    No. The plan, the balance, the limits, and the invoice all belong to the organization. Usage is reported per workspace, but billed as one.
  </Accordion>
  <Accordion title="Can I remove the only Owner?">
    No. Promote a second Owner first, then the original can be demoted or removed.
  </Accordion>
  <Accordion title="How do I delete a workspace?">
    You cannot, from the dashboard. There is no delete action on the workspace list or inside a workspace's own settings. Contact the Future AGI team.
  </Accordion>
  <Accordion title="What happens to someone's work after they are removed?">
    It is preserved. Annotations, scores, and other output remain in place, and access ends immediately.
  </Accordion>
  <Accordion title="An invite expired. What now?">
    Resend it from their row on the Members page. Resending issues a fresh link and restarts the window. Do the same if the invite still shows as Pending but the person says the link is dead.
  </Accordion>
</AccordionGroup>

## Keep exploring

<CardGroup cols={2}>
  <Card title="User Management" href="/docs/admin-settings/user-management" icon="users">
    Invite people, change roles, and deactivate members
  </Card>
  <Card title="Workspace Management" href="/docs/admin-settings/workspace-management" icon="layer-group">
    Create workspaces and manage who belongs to them
  </Card>
  <Card title="API Keys" href="/docs/admin-settings/api-keys" icon="shield">
    Create, rotate, and revoke the keys the SDK uses
  </Card>
  <Card title="Billing & Pricing" href="/docs/admin-settings/billing-pricing" icon="chart-mixed">
    Plans, usage, and what each one includes
  </Card>
</CardGroup>
