---
title: "Scenarios"
description: "The test case a simulation runs: a situation, a flow, and the rows that fill it"
---


## What a scenario is

A **scenario** is one test case for your [agent definition](/docs/simulation/concepts/agent-definitions): the situation a conversation starts from and the flow it should follow. A refund request, a booking change, a billing dispute, each is a scenario the simulator can run.

<div style={{ display: 'flex', justifyContent: 'center', margin: '1.5rem 0' }}>
  <img src="/images/docs/simulation/simulation-model-scenario-highlighted.svg" alt="Your agent holds a conversation with the FAGI simulator inside a simulated environment. A list of scenarios feeds the environment, and a list of personas feeds the scenarios." style={{ border: 'none', width: '100%', maxWidth: '860px', margin: '1.75rem auto', display: 'block' }} />
</div>

In a run, the simulator loads one scenario from this list and plays it out against your agent. The rest of this page is what a single scenario holds.

## One scenario, many conversations

A scenario is not a single fixed script. It carries a table of rows, and each row, a different customer, amount, or objection, plays out as its own conversation from the same flow. That is how one refund request scenario becomes a hundred concrete tests instead of one.

## The simulator prompt

Every scenario carries a **simulator prompt**: the instructions the simulator follows to play the customer. It draws on each row's values, so the customer it plays shifts from row to row while the situation the scenario describes stays fixed.

## Global nodes

A **global node** is a conversation step the agent can reach at any point, not only when the flow arrives at it. It fits anything that can interrupt at any moment: an off-topic question, a sudden pricing query, a "talk to a human" request. One global node covers that case from anywhere in the flow, and only conversation nodes can be global.

## Scenarios are reusable

Because a scenario is defined once and pinned to a run, the same set plays against every agent version. Kept together, they become a regression suite: run them after each change, and any conversation that used to pass and now fails stands out. To build your first one, see [Create scenarios](/docs/simulation/guides/create-scenarios).

## Keep exploring

<CardGroup cols={3}>
  <Card title="Create scenarios" icon="list-check" href="/docs/simulation/guides/create-scenarios">
    Build one from a workflow graph, a dataset, a script, or an SOP
  </Card>
  <Card title="Personas" icon="users" href="/docs/simulation/concepts/personas">
    The customer that plays out the scenario
  </Card>
  <Card title="Runs & results" icon="chart-mixed" href="/docs/simulation/concepts/runs-and-results">
    How scenarios turn into scored calls
  </Card>
</CardGroup>
