---
title: "Overview"
description: "What a dataset is made of, where the data comes from, and where to go next"
---

## What is a Dataset?

A **dataset** is a table of examples. [Prompts](/docs/prompt), [evals](/docs/evaluation), experiments, and [optimization](/docs/optimization) all run against it, and the results land back in the same table as new columns. You reach it from **Dataset** in the left nav.

## What's in a dataset

Each dataset is a grid: **columns** define what you're capturing (a query, an expected answer, a score), **rows** are the individual examples, and a **cell** holds the value where a row meets a column. A column's values come from you directly, or get filled in automatically by running something against the dataset. See [Static & Dynamic Columns](/docs/dataset/concepts/static-and-dynamic-columns) for the difference.

## Where the data comes from

- **File upload**: bring in a CSV, JSON, or spreadsheet export
- **The SDK**: push rows from your own code
- **Synthetic generation**: describe a schema and get realistic rows back
- **Hugging Face**: import an existing dataset by name
- **An existing dataset or experiment**: branch off data you already have in Future AGI
- **[Observe](/docs/observe) traces**: turn real production traffic into rows

## Concepts and Guides

<CardGroup cols={2}>
  <Card title="Understanding Datasets" icon="compass" href="/docs/dataset/concepts/understanding-datasets">
    The object model: columns, rows, and cells
  </Card>
  <Card title="Create a dataset" icon="plus" href="/docs/dataset/guides/create-a-dataset">
    Upload a file, use the SDK, or generate one from a schema
  </Card>
</CardGroup>
