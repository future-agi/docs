---
title: "Run a prompt on every row"
description: "Turn a prompt into a column of model output, one response per row, with token counts and response time recorded on every cell."
---

**Run Prompt** fills a new [dynamic column](/docs/dataset/concepts/static-and-dynamic-columns) by running a prompt against every row of a dataset that already exists. You write the prompt once, referencing other columns as inputs, and Future AGI runs it row by row until the whole column is filled.

<Steps>
  <Step title="Open Run Prompt">
    On a dataset's **Data** tab, click **Run Prompt**. This starts a new column and opens the panel where you build the prompt and pick a model.
  </Step>

  <Step title="Build the prompt">
    A prompt is a list of messages with roles:

    - **System** (optional): instructions that set the model's behavior and context
    - **User** (required): the input message, built from your dataset's columns

    Use `{{column_name}}` inside a message to pull that column's value for the current row. Take a dataset with a `user_query` column and a `customer_context` JSON column:

    ```
    System: You are a support assistant that helps resolve customer tickets.

    User: A customer asked: {{user_query}}. Their plan is {{customer_context.plan}}. Write a helpful response.
    ```

    `{{user_query}}` pulls that row's plain text value. `{{customer_context.plan}}` uses dot notation to reach the `plan` field inside the `customer_context` JSON column, without pulling in the rest of that column's value. Name the new column `model_response` for this example. It's what every row's response lands in.
  </Step>

  <Step title="Choose a model type">
    Run Prompt supports four kinds of models, each shaped the same way: pick one, and the column fills with what that type of model produces.

    | Model type | Input | Output |
    | --- | --- | --- |
    | LLM | The prompt you built above | Text |
    | Text-to-Speech | A text column referenced in the prompt | Audio |
    | Speech-to-Text | An audio column referenced in the prompt | Transcribed text |
    | Image Generation | The prompt you built above | An image |

    Don't see the model you need? [Register a custom model](/docs/evaluation/guides/custom-models) and it joins the same list.
  </Step>

  <Step title="Configure model parameters (optional)">
    Adjust the model's generation parameters if the defaults don't fit your prompt.
  </Step>

  <Step title="Configure tools (optional)">
    Attach tools the model can call while it runs, if your prompt needs them.
  </Step>

  <Step title="Set concurrency">
    Set how many rows run at once. Higher concurrency finishes the column faster but pushes more requests at your model provider in parallel.
  </Step>

  <Step title="Run">
    Click **Run**. Future AGI works through the dataset row by row and writes each response into the new column.
  </Step>
</Steps>

## What lands in the column

The new column is dynamic, the same mechanism [Static & Dynamic Columns](/docs/dataset/concepts/static-and-dynamic-columns) covers for any producer: its `source` is recorded as `run_prompt`, and `source_id` points at the run behind it.

While a row's call is in flight, its cell shows running. If the call fails, it shows error. Otherwise it settles to pass, the default state, with the response written into it.

Each cell an LLM produces also records `prompt_tokens`, `completion_tokens`, and `response_time`, so you can check cost and latency per row without leaving the dataset. A Text-to-Speech cell carries `audio_duration_seconds` in its `column_metadata` alongside the audio itself.

## Dive deeper

<CardGroup cols={3}>
  <Card title="Add columns" icon="plus" href="/docs/dataset/guides/add-columns">
    Add a static or dynamic column from the Data tab
  </Card>
  <Card title="Dynamic column methods" icon="list-check" href="/docs/dataset/reference/dynamic-column-methods">
    Every other producer a dynamic column can point at
  </Card>
  <Card title="Run an experiment" icon="flask" href="/docs/dataset/guides/run-an-experiment">
    Compare prompts and models against each other using evals
  </Card>
</CardGroup>
