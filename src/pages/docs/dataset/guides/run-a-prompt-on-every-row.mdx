---
title: "Run a prompt on every row"
description: "Generate a column of model output for every row in a dataset using a prompt with column placeholders"
---

Run Prompt turns a prompt into a new column. Point it at an LLM, a Text-to-Speech, a Speech-to-Text, or an Image Generation model, and it runs once per row, filling in a value for every row in the dataset. The result is a [dynamic column](/docs/dataset/concepts/static-and-dynamic-columns): one producer, one column, every cell filled the same way.

It's the same producer the [Add columns](/docs/dataset/guides/add-columns) drawer offers under Dynamic Columns. This page walks it end to end.

Open a dataset's **Data** tab and click **Run Prompt**.

## Build the prompt

A prompt is a list of messages, each carrying a role:

- **System** (optional): instructions that set the model's behavior and context for every row
- **User** (required): the input the model actually responds to

Use `{{column_name}}` inside either message to pull that row's value from another column. Future AGI swaps in the cell's value before the call goes out, once per row.

Take a `support-tickets` dataset with a `ticket_text` column holding the customer's message, and a `context` column holding JSON like `{"priority": "high", "customer_tier": "enterprise"}`. A prompt that triages each ticket:

```
System: You are a support agent triaging incoming tickets.

User: Summarize this ticket in one sentence: {{ticket_text}}
```

For a JSON column, dot notation reaches straight into a nested field:

```
User: This is a {{context.priority}} priority ticket. Summarize it in one sentence: {{ticket_text}}
```

`{{context.priority}}` pulls the `priority` field out of the `context` column without you extracting it into its own column first.

Name the new column, say `triage_summary`. Every row gets one cell in it, holding that row's response.

## Pick a model type

Run Prompt supports four kinds of model, and each takes a different kind of input and leaves a different kind of output:

| Model type | Takes | Produces |
|---|---|---|
| LLM | The rendered prompt | Text, or structured JSON if you set a response format |
| Text-to-Speech | Text | Audio, stored as a URL, with its clip duration recorded on the column |
| Speech-to-Text | Audio | Text (the transcript) |
| Image Generation | Text, optionally an image | An image, stored as a URL |

The picker only lists models already available in your workspace. To add one that isn't there, see [Use custom models](/docs/evaluation/guides/custom-models).

## Configure the run

Three settings apply no matter which model type you picked:

- **Model parameters**: fields specific to the model type, for tuning how it generates each row's response
- **Tools**: any tools the model can call while it works through the column
- **Concurrency**: how many rows run at once, trading speed against load

Click **Run** to fill the column.

## What lands in the column

Each cell shows `running` while its call is in flight, then settles on `pass` once the value arrives, or `error` if that row's call failed. An LLM-generated cell also keeps its prompt and completion token counts and the call's response time alongside the value, so you can see what a row actually cost without leaving the grid.

## Dive deeper

<CardGroup cols={3}>
  <Card title="Add columns" icon="plus" href="/docs/dataset/guides/add-columns">
    Every other way to add a static or dynamic column
  </Card>
  <Card title="Static & Dynamic Columns" icon="table-columns" href="/docs/dataset/concepts/static-and-dynamic-columns">
    What makes this column dynamic, and what breaks if you delete it
  </Card>
  <Card title="Run an experiment" icon="flask" href="/docs/dataset/guides/run-an-experiment">
    Compare configurations across the dataset using evals
  </Card>
</CardGroup>
