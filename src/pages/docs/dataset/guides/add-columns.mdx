---
title: "Add columns"
description: "Add a static or dynamic column to a dataset from the same drawer"
---

Every column starts the same way, whether you're typing the values in yourself or asking Future AGI to compute them. Open the drawer, name the column, tell it what should fill the cells, and create it.

## Open Add Columns

Open a dataset's **Data** tab and click **+ Add Columns**. The panel opens on three tabs: **All**, **Static Columns**, and **Dynamic Columns**. All lists every column type in one search; the other two narrow it down.

## Add a static column

Pick a [static column](/docs/dataset/concepts/static-and-dynamic-columns) when you already know the value and want to enter it yourself, a reference label or a fixed expected answer.

Under **Static Columns**, choose a data type, Text and Integer among them, name the column, and click **Add Column**. It lands in the grid empty, ready for you to fill or paste values into.

## Add a dynamic column

Pick a dynamic column when the value should be computed for you instead of typed in. Under **Dynamic Columns**, pick a producer, **Classification** for this example, and a dedicated panel opens for it.

Name the column, then configure Classification: which column it reads and which categories it sorts each row into. Click **Test** to preview the result on a sample of rows, then **Create New Column** to run it across the whole dataset.

Classification is one producer among several. [Run a prompt on every row](/docs/dataset/guides/run-a-prompt-on-every-row) walks the most common one, Run Prompt, end to end. [Dynamic column methods](/docs/dataset/reference/dynamic-column-methods) covers what the rest, retrieval, API calls, JSON extraction, and conditional branching among them, ask for.

## Naming rules

Future AGI checks a column's name before it creates anything:

- A name over 255 characters is rejected
- A name already used elsewhere in the dataset is rejected
- Two columns with the same name in one request are rejected, even if neither exists in the dataset yet

Rename or drop the clashing column, then try again.

## Dive deeper

<CardGroup cols={2}>
  <Card title="Run a prompt on every row" icon="play" href="/docs/dataset/guides/run-a-prompt-on-every-row">
    Fill a column by running a prompt against every row
  </Card>
  <Card title="Dynamic column methods" icon="list-check" href="/docs/dataset/reference/dynamic-column-methods">
    The fields each producer asks for, beyond Run Prompt and Classification
  </Card>
  <Card title="Limits & Data Types" icon="table-columns" href="/docs/dataset/reference/limits-and-data-types">
    Every supported data type, and the caps that apply to a dataset
  </Card>
  <Card title="Run an experiment" icon="flask" href="/docs/dataset/guides/run-an-experiment">
    Compare columns against each other using evals
  </Card>
</CardGroup>
