---
title: "Feedback"
description: "Record corrections and human signals on eval results to improve your evaluator over time"
---

## About

**Feedback** is a human or system signal recorded on an [evaluation](/docs/evaluation) result, marking whether you agree with the judgment. When an eval returns a wrong result, you submit feedback with the correct verdict and an optional explanation. That feedback is stored and used to improve future evals: it drives the correction loop so custom evals learn your domain's definition of quality, and it supplies few-shot examples that steer LLM-based evals toward your standards.

Feedback turns scattered disagreements into systematic improvement, so evaluators converge on what your team means by correct.

---

## How feedback is captured

Each feedback entry records:

| Field | What it holds |
|---|---|
| **Value** | Your verdict: `passed` or `failed`, what the eval should have returned |
| **Explanation** | Why you are correcting the eval, optional, up to 5000 characters |
| **Improvement suggestion** | How the eval could be better, optional, up to 5000 characters |
| **Source** | Where the eval ran: `dataset`, `trace`, `experiment`, `eval_playground`, `prompt`, `observe`, or `sdk` |
| **Source ID** | The row, trace, or item the feedback refers to |

Feedback is stored in the `Feedback` model alongside the [eval template](/docs/evaluation/concepts/eval-templates), the person who submitted it, and the evaluated row, so you can retrieve every correction for a given eval.

---

## The correction loop

The correction loop is the pattern that turns feedback into a better eval: run an eval, find rows where it disagrees with your judgment, record the corrections, rewrite the eval rules to include those corrections as examples, then re-run and confirm agreement climbs.

1. **Establish a baseline**: run a built-in eval on a batch of rows and compare its results to your manual verdicts.
2. **Find disagreements**: a disagreement is any row where the eval and your team reach different verdicts. These rows teach the evaluator something new.
3. **Encode corrections**: write a custom eval whose rule prompt states your domain rules and includes a few disagreement rows as few-shot examples for the judge model.
4. **Re-score and measure**: run the new eval on the same batch and measure agreement against your verdicts.
5. **Iterate**: pull a fresh batch, add new examples, and increment the version. Most evals converge in a few rounds.

See the [eval correction loop cookbook](/docs/cookbook/evaluation/eval-correction-loop) for a worked end-to-end example.

---

## How feedback feeds back into evals

Feedback improves evals in two ways.

**Threshold calibration.** For custom evals, corrections are collected in a feedback store. Calibration sweeps threshold values and picks the one that best matches your corrections, tightening the eval's pass and fail boundary.

**Few-shot injection.** For LLM-based evals, the SDK retrieves similar past corrections and injects them into the judge prompt as examples. Instead of a generic prompt, the judge sees what good and bad look like for your domain before it scores.

The [SDK feedback reference](/docs/sdk/evals/feedback) covers the collector, calibration, retrieval, and storage options in detail.

---

## Sources

Feedback can come from any surface where you evaluate, so you can close the loop wherever the eval ran:

| Source | Comes from | Use case |
|---|---|---|
| **Dataset** | Dataset column results | Labeling rows where bulk evals got it wrong |
| **Trace** | Live traces in Observe | Marking production responses that were misscored |
| **Experiment** | Experiment results | Marking experiment outcomes that did not match expectations |
| **Eval playground** | The eval tester | Quick feedback on an eval without running it on data |
| **SDK** | Programmatic collection | Collecting corrections at scale in scripts or notebooks |

---

## Next steps

- [Eval templates](/docs/evaluation/concepts/eval-templates): Write the rule prompts and criteria that feedback refines.
- [Eval correction loop](/docs/cookbook/evaluation/eval-correction-loop): A worked example of turning corrections into a better eval.
- [SDK feedback reference](/docs/sdk/evals/feedback): Calibration, retrieval, and storage in the SDK.
