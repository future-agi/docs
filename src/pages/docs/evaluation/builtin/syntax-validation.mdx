---
title: "Syntax Validation: Built-in Evaluation"
description: "Validates code syntax without executing it. Supports Python (via ast.parse), JSON, and basic JavaScript bracket validation. Useful for checking if LLM-genera..."
---

Validates code syntax without executing it. Supports Python (via ast.parse), JSON, and basic JavaScript bracket validation. Useful for checking if LLM-generated code is syntactically correct.

<CodeGroup>

```python Python
result = evaluator.evaluate(
    eval_templates="syntax_validation",
    inputs={
        "text": "Hello, this is a sample text."
    },
)

print(result.eval_results[0].output)
print(result.eval_results[0].reason)
```

```typescript JS/TS
import { Evaluator } from "@future-agi/ai-evaluation";

const evaluator = new Evaluator();

const result = await evaluator.evaluate(
  "syntax_validation",
  {
    text: "Hello, this is a sample text."
  }
);

console.log(result);
```

</CodeGroup>

| **Input** |  |  |  |
| ------ | --------- | ---- | ----------- |
| | **Required Input** | **Type** | **Description** |
| | `text` | `string` | The text. |

| **Output** |  |  |
| ------ | ----- | ----------- |
| | **Field** | **Description** |
| | **Result** | Returns `Passed` or `Failed` per row, with a reason explaining the verdict. |
| | **Reason** | A plain-language explanation of the verdict. |

**Tags:** `Code`, `Output Validation`
