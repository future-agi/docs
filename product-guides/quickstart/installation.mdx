---
title: "Installation"
description: "In this section, you'll find comprehensive step-by-step instructions on how to install the Future AGI library. It includes details on prerequisites, necessary tools, and commands to set up the environment for optimal performance."
---

To install the Future AGI client, you have two flexible options: you can either clone the repository directly from GitHub or install the library using pip. Below are the detailed steps for both methods, along with additional information to ensure a smooth installation process:

Install Library using pip:

To install the library, ensure that you are using Python version 3.6 or higher. You can install the library in your environment by running the following command:

```python
pip install futureagi
```

Clone the Repository:

Alternatively, you can clone the repository to have direct access to the source code. Use the following git command to clone the repository:

```python
 git clone https://github.com/future-agi/client
```

Choose the method that best fits your workflow and follow the steps to set up the client.


